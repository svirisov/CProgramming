#include <stdio.h>

int main (void){
	double speed;
	double distance = 22;
	double time = 5;
	speed = distance/time;
	printf("distance = %f, time = %f, speed = %f\n", distance, time, speed);
	return 0;
}
